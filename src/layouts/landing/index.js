/*
 * Layout: Index
 */
import React from 'react';
const LandingLayout = (props) => {
    const { children } = props;
    return (
        <React.Fragment>
            {children}
        </React.Fragment>
    );
};
export default LandingLayout;
